# MedGuide-AI
MedGuide AI — это чат-бот на основе искусственного интеллекта, способный анализировать загруженные клинические рекомендации на русском языке, извлекать из них методы лечения и вести диалог с врачом, задавая уточняющие вопросы для подбора оптимальной тактики терапии.

---

## Архитектура (REST)

Проект состоит из трёх основных частей:
- **Backend (Python)** — обработка клинических рекомендаций и логика чат-бота
- **Frontend (Flutter)** — интерфейс взаимодействия с врачом
- **ИИ-компонент** — извлечение лечения и генерация вопросов

**Backend** построен на FastAPI и включает в себя:
- Загрузку и обработку документов (`/api/v1/documents`)
- Чат-сессии (`/api/v1/chat`)
- Статус и health-check (`/api/v1/health`)
- Обработку PDF/DOCX/HTML через `services/ingestion/`
- Извлечение лечения через LLM (Qwen) в `services/extraction/`
- Хранение данных через `storage/`
- Простую очередь задач в `jobs.py`

**Frontend** реализован на Flutter и включает:
- Экран загрузки документа
- Список обработанных документов
- Чат-экран с врачом
- Просмотр извлечённых данных

**ИИ-компонент** использует:
- Qwen API через `llm/qwen_adapter.py`
- Промпты для классификации и извлечения в `services/extraction/prompts/`
- Постобработку и валидацию результатов

---
## API endpoints


## 📱 Frontend (Flutter)

**Основные экраны:**

- **Загрузка документа** — интерфейс для загрузки клинической рекомендации (PDF/DOCX/HTML).
- **Список документов** — отображение загруженных документов и их статуса обработки.
- **Чат-экран** — интерфейс для взаимодействия с врачом, отображение уточняющих вопросов и рекомендаций.
- **Просмотр извлечённой структуры** — просмотр всех извлечённых данных (способы лечения, decision graph, источники).

## 🧱 Backend (FastAPI, без auth)

### 📡 Эндпоинты

| Метод | Путь | Описание |
|------|------|----------|
| `POST` | `/api/v1/documents` | Загрузка документа (multipart/form-data: file) |
| `GET` | `/api/v1/documents/{id}` | Получение статуса и метаданных документа |
| `GET` | `/api/v1/documents/{id}/extraction` | Получение извлечённой структуры (лечения, decision graph, цитаты) |
| `POST` | `/api/v1/chat/sessions` | Создание новой чат-сессии по документу |
| `POST` | `/api/v1/chat/sessions/{session_id}/message` | Обработка ответа врача, возвращает следующий вопрос, рекомендации, состояние |
| `GET` | `/api/v1/health` | Проверка состояния сервиса |

## 📄 Обработка документов

### 🕒 Фоновые задачи
- Обработка задач в памяти через `jobs.py` (без Celery или брокеров сообщений)

### 📂 Парсинг
- **PDF**: `PyMuPDF`
- **DOCX**: `python-docx`
- **HTML/текст**: парсинг напрямую

### 🔀 Сегментация
- Разделение текста на логические части по заголовкам и структуре
- Использование эвристик + при необходимости LLM-классификация для уточнения

### 🧠 Извлечение данных (LLM: Qwen 3-235B Instruct)
Извлекаются следующие сущности:

- **Схемы лечения**: название, линия, показания, противопоказания, дозировка, условия применения
- **Decision graph**: переменные, ветвления «если/то», логика выбора лечения
- **Цитаты**: источники из текста (раздел, страница, фрагмент)

### ✅ Валидация
- Структура данных строго определена через Pydantic-модели
- Постобработка и нормализация значений

## 🗃️ Хранилище

- **По умолчанию**: файловое (каталог `storage/`)
  - JSON-файлы с извлечённой информацией
  - JSON-файлы с состоянием чат-сессий
- **Опционально**: SQLite (включается через `STORAGE_BACKEND=sqlite`)

## 🔍 RAG (опционально)

- **По умолчанию**: отключён (`RAG_MODE=none`)
- **Возможность расширения**: можно подключить `FAISS` или `Qdrant` через адаптеры
- **Без изменения API**: RAG-модуль реализован как плагин

## 🤖 LLM

- **Модель**: Qwen 3-235B Instruct (через API)
- **Переменные окружения**:
  - `QWEN_API_URL` — адрес API
  - `QWEN_API_KEY` — ключ авторизации
- **Промпты**: на русском языке (описаны в `docs/prompts.md`)
---
## API-examples
### Загрузка документа:

```http
POST /api/v1/documents
Content-Type: multipart/form-data

(file upload)
```

Ответ:
```json
{
  "id": "doc_123",
  "status": "queued"
}
```

### Статус обработки:

```http
GET /api/v1/documents/doc_123
```

Ответ:
```json
{
  "id": "doc_123",
  "status": "processed",
  "filename": "clinical_recommendation.pdf"
}
```

### Извлечённая информация:

```http
GET /api/v1/documents/doc_123/extraction
```

Пример ответа:
```json
{
  "treatments": [
    {
      "name": "Препарат X",
      "line": "первая линия",
      "indications": ["..."],
      "contraindications": ["..."],
      "dosage": {"amount": "10 мг", "schedule": "1 раз/день"},
      "conditions": {"стадия": "II-III", "креатинин": "<1.5"},
      "sources": [{"section": "Лечение", "page": 12, "quote": "..." }]
    }
  ],
  "decision_graph": {
    "variables": [{"name": "стадия"}, {"name": "креатинин"}],
    "nodes": [
      {"if": {"стадия": "II-III"}, "then": ["Препарат X"], "else": ["Препарат Y"]}
    ]
  }
}
```

### Создать чат-сессию:

```http
POST /api/v1/chat/sessions
Content-Type: application/json

{
  "document_id": "doc_123"
}
```

Ответ:
```json
{
  "session_id": "sess_456",
  "next_question": "Укажите стадию заболевания?"
}
```

### Отправить сообщение в чат:

```http
POST /api/v1/chat/sessions/sess_456/message
Content-Type: application/json

{
  "answer": {
    "стадия": "III"
  }
}
```

Ответ:
```json
{
  "state": { "стадия": "III" },
  "next_question": "Укажите уровень креатинина?",
  "recommendations": [
    {
      "treatment": "Препарат X",
      "rationale": "Подходит для стадии II-III",
      "citations": [{"page": 12, "quote": "..."}]
    }
  ]
}
```

---

## 📁 Структура проекта

```
root/
├── backend/                
│   ├── app/                
│   │   ├── api/          
│   │   │   └── v1/          
│   │   │       ├── documents.py
│   │   │       ├── chat.py
│   │   │       └── health.py
│   │   ├── core/          
│   │   │   ├── config.py
│   │   │   ├── jobs.py
│   │   │   └── logging.py
│   │   ├── models/      
│   │   │   ├── document.py
│   │   │   ├── section.py
│   │   │   ├── treatment.py
│   │   │   ├── decision_graph.py
│   │   │   └── chat.py
│   │   ├── services/        
│   │   │   ├── ingestion/ 
│   │   │   │   ├── parser_pdf.py
│   │   │   │   ├── parser_docx.py
│   │   │   │   ├── parser_html.py
│   │   │   │   └── segmenter.py
│   │   │   ├── extraction/ 
│   │   │   │   ├── prompts/  
│   │   │   │   ├── extractor.py
│   │   │   │   ├── validator.py
│   │   │   │   └── postprocess.py
│   │   │   ├── llm/          
│   │   │   │   └── qwen_adapter.py
│   │   │   ├── citations/    
│   │   │   │   └── linker.py
│   │   │   └── storage/     
│   │   │       ├── fs.py
│   │   │       └── sqlite.py
│   │   ├── tests/          
│   │   │   ├── unit/
│   │   │   └── integration/
│   │   ├── main.py          
│   │   ├── pyproject.toml   
│   │   ├── uv.lock         
│   │   ├── Dockerfile        
│   │   └── .env.example  
│   │
├── frontend/                 
│   ├── lib/                 
│   │   ├── main.dart
│   │   ├── screens/
│   │   │   ├── upload_screen.dart
│   │   │   ├── documents_list_screen.dart
│   │   │   ├── chat_screen.dart
│   │   │   └── extraction_view_screen.dart
│   │   ├── services/
│   │   │   └── api_client.dart
│   │   ├── models/
│   │   │   └── dto.dart
│   │   ├── state/
│   │   │   └── providers.dart
│   │   ├── test/             
│   │   ├── assets/          
│   │   ├── pubspec.yaml     
│   │   └── Dockerfile        
│   │
├── infra/                    
│   └── docker-compose.yaml   
│
├── scripts/    
│   ├── dev/
│   │   ├── run_local.sh
│   │   └── seed_example.sh
│
├── docs/    
│   ├── architecture.md
│   ├── api.md
│   └── prompts.md
│
├── .github/                  # CI/CD и шаблоны
│   ├── workflows/
│   │   └── ci.yml            # Линтеры + тесты
│   ├── PULL_REQUEST_TEMPLATE.md
│   ├── ISSUE_TEMPLATE.md
│   └── CODEOWNERS
```

---

## Ветки в Git

- `master` — стабильная версия
- `dev` — основная ветка разработки
- `feature/<название>` — фичи
- `bugfix/<описание>` — исправления
- `hotfix/<описание>` — срочные фиксы

### Правила:
1. Все разработчики работают в `feature` ветках.
2. РАБОТАЕМ ТОЛЬКО В ВЕТКАХ local, в feature делаем PR только после теста на локале
2. MR создаётся из `feature` в `dev`.
3. После ревью и тестирования MR мержится в `dev`.
4. DevOps отвечает за MR из `dev` в `master`.

